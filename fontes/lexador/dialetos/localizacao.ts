/**
 * `Localizacao` é um tipo de objeto que explica a todos os elementos de Delégua como uma linha
 * deve ser processada.
 */
export interface Localizacao {
    linha: number;
    espacosIndentacao?: number;
}
